<?xml version="1.0" encoding="utf-8"?>
<!-- Native launch splash background — Redesign Phase 7. Solid brand
     near-black (see values/colors.xml); replaces the unmodified Flutter
     template's white default. No logo bitmap here (would need new asset
     generation across every mip density) — the Flutter-side
     AppSplashScreen (lib/core/widgets/app_splash_screen.dart) carries the
     actual branding moment immediately after this. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/splash_background" />
</layer-list>
