<?xml version="1.0" encoding="utf-8"?>
<!-- Same brand near-black as drawable/launch_background.xml — see that
     file's doc. Kept as a literal colour reference rather than
     `?android:colorBackground` so the launch screen is never at the
     mercy of a device theme; the app is dark-only by design (Redesign
     Phase 1), not theme-dependent. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/splash_background" />
</layer-list>
